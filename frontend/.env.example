# Copy to .env.local.
#
# The FastAPI backend's address. Deliberately NOT prefixed with NEXT_PUBLIC_:
# only variables carrying that prefix are bundled into browser code, and the
# browser never calls the API directly. Every request goes through this app's
# server, which holds the session token.
API_URL=http://127.0.0.1:8000
