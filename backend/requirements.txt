# Runtime dependencies only. Test tooling lives in requirements-dev.txt so the
# production image doesn't carry it.
#
# These are the packages the project imports directly; pip resolves the rest.
# A lock file (uv, pip-tools) would pin transitive versions too - worth adding if
# builds ever need to be byte-for-byte reproducible.

fastapi==0.141.1
uvicorn[standard]==0.52.4

# Database
sqlmodel==0.0.39
asyncpg==0.31.0
alembic==1.19.1

# Configuration
pydantic-settings==2.15.0

# Auth
pwdlib[argon2]==0.3.1
PyJWT==2.13.0
python-multipart==0.0.32
email-validator==2.3.0
itsdangerous==2.2.0

# Sessions and mail
redis==8.1.0
aiosmtplib==5.1.2
